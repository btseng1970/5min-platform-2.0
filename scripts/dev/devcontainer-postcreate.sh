#!/usr/bin/env bash
set -euo pipefail

echo "5min-platform-2.0 devcontainer ready."
echo "PostgreSQL 16 is reachable at \$DATABASE_URL (service host: postgres)."
echo "Run scripts/dev/run-checks.sh from the host (with Docker) to execute the FND-000 environment checks."
